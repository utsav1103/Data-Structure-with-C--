#include <stdio.h>

int main (){
    //we are getting '!' this in result because we are suing char and giving a value that hold this character
    char var = 33;
    printf("%c", var);
    return 0;
}